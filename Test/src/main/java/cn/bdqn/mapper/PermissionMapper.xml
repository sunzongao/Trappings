<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bdqn.mapper.PermissionMapper">
	<select id="queryAllPer1" resultType="Permission">
		select * from permission where grade=1
	</select>
	<select id="queryAllPer2" resultType="Permission">
		select * from permission where grade=2
	</select>
</mapper>