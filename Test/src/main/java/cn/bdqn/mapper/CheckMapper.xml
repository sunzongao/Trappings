<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.bdqn.mapper.CheckMapper">
	<select id="queryCheck" resultType="Check">
		SELECT
		c.checkId,
		c.checkTime,
		c.storehouseId,
		c.userId,
		s.stoName AS sStoName,
		u.id,u.surname
		FROM
		`check` AS c
		INNER JOIN storehouse AS s ON c.storehouseId = s.storehouseId
		INNER JOIN `user` as u on c.userId=u.id
		<trim prefix="where" prefixOverrides="and | or">
			<if test="storehouseId!=0">
				 c.storehouseId=#{storehouseId}
			</if>
			<if test="checkTime!=null and checkTime!=''">
				and c.checkTime like CONCAT('%',#{checkTime},'%')
			</if>
		</trim>
	</select>

</mapper>